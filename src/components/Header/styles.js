import theme from '../theme';

const { palette, spacing } = theme;

const styles = {
  title: {
    fontWeight: 600,
  },
  drawerSwitch: {
    marginRight: spacing(2),
  },
};

export default styles;
